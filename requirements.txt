sqlalchemy
geoalchemy2
shapely  # still needed?
psycopg2-binary  # still needed?
zeep  
tqdm
geopandas  # still needed?
pyyaml  # still needed?
requests
keyring

# xml download
pandas >=1.4
clint
beautifulsoup4

# TODO: for 'bulk' downlaod add 'clint', 'bs4', pandas == 1.3.0 to enable read_xml()
# TODO: Delete unnecessary requirements for v1.0.0
